services:
  app:
    build: .
    command: bundle exec rackup --host 0.0.0.0 -p <%= @port %>
    ports:
      - "<%= @port %>:<%= @port %>"
    volumes:
      - ./<%= @app_name %>:/db
    <%- if @redis %>
    redis:
      image: redis:7.0-alpine
      ports:
        - 6379:6379
    <%- end %>
